# Tubes2_BOELANGOSONK-V2
🌳 A React + Go web app for parsing HTML into a visual DOM tree data structure, enabling users to query, trace, and animate element traversal paths using Breadth-First Search (BFS) and Depth-First Search (DFS) algorithms based on custom CSS selectors.
